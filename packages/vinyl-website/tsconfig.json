{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        // Website-specific: no emit (vite builds), not a referenced project,
        // and transpiled per-file by vite/oxc so isolatedModules must hold.
        "composite": false,
        "declaration": false,
        "declarationMap": false,
        "isolatedModules": true,
        "resolveJsonModule": true,
        // Relative imports carry explicit .ts extensions so Vite's native
        // config loader can resolve them; noEmit makes this safe.
        "allowImportingTsExtensions": true,
        "noEmit": true
    },
    "include": ["src", "buildSrc", "vite.config.ts"]
}
