# Unit Tests

Unit tests are written to run in NODE or a browser, they should test isolated
pieces of code while mocking all dependencies within reason.

Browser API mocks can be found in @amazon/vinyl-util/browserTestUtil.
